export class Document {
    heading: string;
    description: string;

    constructor(title: string, content: string,) {
        this.heading = title;
        this.description = content;
        
    }
}
